{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Everything the popover is allowed to do. Deliberately short: there is no network layer, no filesystem plugin, and no shell. The pet is not listed because it has no webview to make a request from.",
  "windows": [
    "popover"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "core:window:allow-hide",
    "dialog:allow-open",
    "clipboard-manager:allow-write-image"
  ]
}
